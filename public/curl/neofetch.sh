#!/bin/bash


SCRIPT_DIR="/var/task/public/curl"
# SCRIPT_DIR="public/curl"

source "$SCRIPT_DIR/config.sh"


printf "%s          .--.         %spramod@portfolio\n" "$C_YELLOW" "$C_CYAN"
printf "%s         |o_o |        %s-----------------\n" "$C_YELLOW" "$C_DEFAULT"
printf "%s         |:_/ |        %sOS%s: Arch Linux x86_64\n" "$C_YELLOW" "$C_GREEN" "$C_DEFAULT"
printf "%s        //   \ \       %sShell%s: bash/zsh\n" "$C_YELLOW" "$C_GREEN" "$C_DEFAULT"
printf "%s       (|     | )      %sEditor%s: Neovim\n" "$C_YELLOW" "$C_GREEN" "$C_DEFAULT"
printf "%s      /'\\_   _/'\\      %sLanguages%s: Python, TypeScript, Bash, PHP\n" "$C_YELLOW" "$C_GREEN" "$C_DEFAULT"
printf "%s      \\___)=(___/      %sStatus%s: All systems nominal\n" "$C_YELLOW" "$C_GREEN" "$C_DEFAULT"
printf "%s                       %sDE%s: Hyprland\n\n" "$C_YELLOW" "$C_GREEN" "$C_DEFAULT"
printf "%sDescription%s: Minimalist dev building terminal-inspired tools\n" "$C_MAGENTA" "$C_DEFAULT"
printf "%sQuote%s:       Minimal. Efficient. Terminal-inspired.\n\n" "$C_MAGENTA" "$C_DEFAULT"

printf "%s[INFO]%s Portfolio loaded successfully..\n" "$C_GREEN" "$C_DEFAULT"
printf "%s[OK]%s   System running smoothly..\n" "$C_GREEN" "$C_DEFAULT"
